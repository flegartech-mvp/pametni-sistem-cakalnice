# Smart Queue System — PametniSistemCakalnica
# This app runs entirely in the browser — no backend, no API keys required for the demo.
# Add variables here if you wire up a real backend in the future.

# Example: if you add an API backend
# VITE_API_URL=http://localhost:8000

# Example: if you add analytics
# VITE_ANALYTICS_KEY=
