# PametniSistemCakalnice — README Headline

## Primary Headline

```
Browser-based queue management system for clinics and reception desks — no backend required
```

## Alternatives

1. `Manage patient queues, display wait times, and check in via QR — fully client-side React app`
2. `Smart Queue System: staff panel + waiting room display + QR patient status, zero backend, Playwright tested`
3. `Replace paper queue slips with a React TypeScript SaaS interface — ships with three role-based views`
